import Image from "next/image";

type Certification = { id: number; title_fa: string; title_en: string; issuer: string; certificate_code: string; file: string | null };
type Honor = { id: number; title_fa: string; title_en: string; issuer: string; year_label: string; description_fa: string; description_en: string; image: string | null };

export function CompanyCertificationCards({ items, locale }: { items: Certification[]; locale: string }) { if (!items.length) return null; const english = locale === "en"; return <section className="about-content-sections" aria-labelledby="about-certifications-title"><header><span>{english ? "Verified records" : "سوابق تأییدشده"}</span><h2 id="about-certifications-title">{english ? "Standards and certifications" : "استانداردها و گواهی‌ها"}</h2></header><div>{items.map((item) => <article key={item.id}><h3>{english && item.title_en ? item.title_en : item.title_fa}</h3>{item.certificate_code && <p><bdi dir="ltr">{item.certificate_code}</bdi></p>}{item.issuer && <p>{item.issuer}</p>}{item.file && <a href={item.file} target="_blank" rel="noreferrer">{english ? "View document" : "مشاهده سند"}</a>}</article>)}</div></section>; }

export function CompanyHonorCards({ items, locale }: { items: Honor[]; locale: string }) { if (!items.length) return null; const english = locale === "en"; return <section className="about-content-sections" aria-labelledby="about-honors-title"><header><span>{english ? "Approved records" : "سوابق تأییدشده"}</span><h2 id="about-honors-title">{english ? "Honors" : "افتخارات"}</h2></header><div>{items.map((item) => <article key={item.id}>{item.image && <Image src={item.image} alt={english && item.title_en ? item.title_en : item.title_fa} width={480} height={280} />}<h3>{english && item.title_en ? item.title_en : item.title_fa}</h3>{item.year_label && <p>{item.year_label}</p>}{(english ? item.description_en || item.description_fa : item.description_fa) && <p>{english ? item.description_en || item.description_fa : item.description_fa}</p>}</article>)}</div></section>; }
