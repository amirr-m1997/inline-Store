"use client";

export function CatalogToolbar({ query, ordering, inStock, resultCount, searchLoading, activeFilterCount = 0, onSearch, onOrderingChange, onStockChange, onOpenFilters }: { query?: string; ordering: string; inStock: boolean; resultCount: number; searchLoading: boolean; activeFilterCount?: number; onSearch: (value: string) => void; onOrderingChange: (value: string) => void; onStockChange: (value: boolean) => void; onOpenFilters: () => void }) {
  void query; void resultCount; void searchLoading; void onSearch;
  return <div className="catalog-toolbar" aria-label="کنترل‌های کاتالوگ"><div className="catalog-filters"><div className="catalog-sort-control"><label htmlFor="catalog-sort-ordering">مرتب‌سازی</label><select id="catalog-sort-ordering" aria-label="مرتب‌سازی" value={ordering} onChange={(event) => onOrderingChange(event.target.value)}><option value="code">کد کالا</option><option value="name">نام کالا</option><option value="-created_at">جدیدترین</option></select></div><label className="stock-filter"><input type="checkbox" checked={inStock} onChange={(event) => onStockChange(event.target.checked)} /> <span>فقط موجود</span></label><button className="spec-filter" type="button" onClick={onOpenFilters} aria-label={`باز کردن فیلترها${activeFilterCount ? `، ${activeFilterCount.toLocaleString("fa-IR")} فیلتر فعال` : ""}`}><span>فیلترها</span>{activeFilterCount > 0 && <b aria-hidden="true">{activeFilterCount.toLocaleString("fa-IR")}</b>}</button></div></div>;
}
