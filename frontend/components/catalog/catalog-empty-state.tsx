export function CatalogEmptyState({ message, query, onClear, locale = "fa" }: { message: string; query?: string; onClear?: () => void; locale?: string }) { const english = locale === "en"; return <div className="empty-state"><b>{query ? (english ? `No results for “${query}”` : `نتیجه‌ای برای «${query}» پیدا نشد`) : (english ? "No products available" : "محصولی موجود نیست")}</b>{query && <p className="catalog-empty-query">{english ? `Search: “${query}”` : `جست‌وجو برای «${query}»`}</p>}<p>{message}</p>{query && onClear && <button type="button" className="catalog-empty-reset" onClick={onClear}>{english ? "Clear search" : "پاک کردن جست‌وجو"}</button>}</div>; }
