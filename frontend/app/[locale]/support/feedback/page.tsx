import type { Metadata } from "next"; import { FeedbackForm } from "../../../../components/content/feedback-form"; import { absoluteUrl, localizedAlternates, localizedPath } from "../../../../lib/locale-url";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;const path="/support/feedback";return {title:locale==="en"?"Customer feedback":"بازخورد مشتری",description:locale==="en"?"Share private feedback about your experience.":"بازخورد خصوصی خود را درباره تجربه‌تان ارسال کنید.",robots:{index:false,follow:true},alternates:{canonical:absoluteUrl(localizedPath(locale,path)),...localizedAlternates(path)}}}
export default async function FeedbackPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const en=locale==="en";return <main className="public-page site-container feedback-page"><header className="content-index-heading"><span>{en?"Customer Service":"امور مشتریان"}</span><h1>{en?"Customer feedback":"ارسال بازخورد"}</h1><p>{en?"Share your private feedback about your experience with our products and service.":"بازخورد خصوصی خود را درباره تجربه استفاده از محصولات و خدمات با ما در میان بگذارید."}</p></header><FeedbackForm english={en}/></main>}
