import type { Metadata } from "next";
import { SupportRequestForm } from "../../../../components/content/support-request-form";
import { absoluteUrl, localizedAlternates, localizedPath } from "../../../../lib/locale-url";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;const path="/support/request";return {title:locale==="en"?"Support request":"ثبت درخواست پشتیبانی",description:locale==="en"?"Submit a customer support request.":"ثبت درخواست پشتیبانی مشتریان.",robots:{index:false,follow:true},alternates:{canonical:absoluteUrl(localizedPath(locale,path)),...localizedAlternates(path)}}}
export default async function SupportRequestPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const en=locale==="en";return <main className="public-page site-container support-request-page"><header className="content-index-heading"><span>{en?"Customer Service":"امور مشتریان"}</span><h1>{en?"Submit a support request":"ثبت درخواست پشتیبانی"}</h1><p>{en?"Submit a request about a product, order, warranty, sales question, complaint, or another supported topic.":"درخواست خود را درباره محصول، سفارش، گارانتی، فروش، شکایت یا یکی از موضوعات پشتیبانی‌شده ارسال کنید."}</p></header><SupportRequestForm english={en}/></main>}
